This folder contains the generated data that was used in the last benchmark run
Unsorted data is in `before`, and sorted data is in `after`
Each file is named after the amount of elements in the array

Both algorithms write to the same file unfortunately, so data from merge sort is on odd lines, and data from selection sort is on even lines
